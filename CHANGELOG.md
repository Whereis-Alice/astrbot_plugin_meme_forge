# Changelog

## v1.10.5 - 2026-09-02

**换源（破坏性）：PJSK 底图库从 359 张升到 787 张**

- 起因：之前钉的上游 `TheOriginalAyaka/sekai-stickers` 已经归档，2023 年之后游戏里新出的角色贴纸一张都没有，所以「素材库是几年前的」是真的。
- 换成 `laffylaffyla/sekai-stickers`（MIT）—— 同一套素材的活跃 fork，画法、画布、排版规则完全一致，只是持续把新贴纸补了进来。原项目仍然写在致谢里。
- 底图 **359 → 787** 张，角色仍是 26 位 —— 游戏主线全员就是这 26 个（5 个组合 20 人 + 6 位虚拟歌手，已对照游戏主数据核实），所以增量全部落在每个人的表情张数上。
- 每位角色从 13 ~ 16 张涨到 **25 ~ 41 张**，中位数 30。最多的是初音未来（13 → 41），最少的是巡音流歌（13 → 25），没有任何一位角色的张数减少。
- 旧的一套是「每人固定 13 张 + 少数人多 1 ~ 3 张」，编号里还整齐地缺 05、10、15；新的一套按角色实际拥有的贴纸连续编号，缺口一并补上。
- 顺带补回旧 pin 本身就漏掉的 6 张素材（`airi/Airi_17`、`airi/Airi_18`、`ena/Ena_17` ~ `Ena_19`），并排除上游仓库里的测试残留 `emutest.png`。
- 相应调整：下载体积 ~22 MB → ~57 MB（连字体约 65 MB），安装前的磁盘余量检查 96 MB → 256 MB，下载超时默认 900 → **1800 秒**（`pjsk_download_timeout`）。
- **升级后需要重新执行一次 `/sk素材安装 确认`**：插件按「已审阅提交」校验素材指纹，旧素材目录会被判定为过期。
- 上游把所有素材重新导出到了统一画布（296×256），意外的好处是排版几何量从此对得死死的 —— 文字锚点 x=148 正好是画布中线，出图比以前更居中。

**序号全部重排，收藏夹自动迁移到稳定键**

- 序号从 1~359 变成 **1~787**，同一个数字指向的图变了。举例：以前的 `/sk 206`（未来3）现在是 `/sk 449`。
- 收藏键跟着换了形式：以前存全局序号（`pjsk:206`），现在存「角色 + 角色内编号」（`pjsk:miku3`）。这样以后再换源、再补图，你收藏的还是那张图，不会跟着数字漂走。
- 旧收藏在读取时自动改写，**359 个老序号里 344 个对上了**，15 个因为上游重画过认不出来。认不出来的不会瞎猜：收藏夹里会明确标「底图已失效」，并提示重新收藏。
- 对不上的老序号：31、109、111、123、150、204、220、222、223、227、251、252、294、353、354。
- 映射表是怎么来的：上游重新导出素材时换掉了文件名，两套编号之间没有任何元数据能对应，所以是拿图本身比出来的 —— 裁到 alpha 边界、白底压平、转灰度、缩到 48×48、z 归一化，再按 RMS 距离比，并且只在同一个角色内部比较。344 条命中的距离中位数 0.04、最差 0.38，而次优候选普遍在 0.9 上下，区分度足够。一个独立验证：旧序号 206（文档里一直写作「未来3」）算出来落在新序号 449，而 449 在新库里恰好也是 `miku3`。
- `/sk miku3`、`/sk 未来3`、`/sk角色 未来` 这些不带全局序号的写法完全不受影响，推荐日常就用这种。

**新增：`/meme工坊更新检查` 多了一行 PJSK**

- 之前只查 `meme_generator`、`meme_emoji`、Gouqi 三个源。现在会额外拉 `laffylaffyla/sekai-stickers` 的最新提交，和插件内「已审阅底图」比对，上游有改动时明确提示需要等适配。字体源同样列出提交号。
- 拉不到（限流、断网）只会显示「检查失败」并写一条 warning，不影响其他几项。

**修复：PJSK 收藏成功后给的命令是拼不出来的**

- 引用 PJSK 贴纸回复 `/meme收藏`，确认消息里的「下次可用」原本会打印 `/meme pjsk 449` —— 它把 PJSK 的内部触发词当成普通 meme 关键词套上了 `/meme` 前缀，照抄发出去只会得到「没找到」。现在和收藏夹列表用同一套逻辑，给的是 `/sk 449 你的文字`。

**修复：失效的老收藏删不掉**

- 迁移不了的那 15 条收藏仍然存着老序号，而 `/meme取消收藏 pjsk 204` 会把 204 按新序号解析，于是回一句「pjsk:ichika1 当前不在收藏夹中」，那条失效收藏怎么都删不掉。现在纯数字先和收藏夹里实际存着的老键对一次，对上就删它，没有这条收藏才按新序号理解。

**其他**

- 新增 9 个单元测试，总数 **192**：787 个稳定键逐个走「写入 → 读回 → 解析回同一张图」的往返、老序号映射抽查、迁移函数幂等（没变化时连列表对象都不换）、失效条目的提示文案与删除路径，以及投递测试改用真实目录里的贴纸而不是手搓的替身对象。
- README 的 PJSK 一节加了升级提示，并改掉了「收藏里的老命令都不用改」这句 —— 换源之后它已经不成立了。

## v1.10.4 - 2026-09-02

**新增：透明输出自动转 GIF，修掉手机 QQ 上的黑块**

- 现象：QQ 手机版不渲染 PNG 的 alpha 通道，透明背景会被填成一整块黑色。PJSK 贴纸整张都是镂空的，受影响最明显；`meme_emoji`、Gouqi 和自制模板里的透明底模板同样中招。桌面端和 Web 端显示正常，所以很容易被当成插件出图坏了。
- 处理：新增配置 `transparent_output_as_gif`（**默认开**）。发送前先检查成图里是否真的存在非不透明像素，只有存在时才重新编码成带透明色的 GIF —— GIF 用调色板里的一个索引表达镂空，手机端就能正常显示。
- 不会牵连其他图：已经是 GIF、JPEG 的输出，以及完全不透明的静态图，都按原字节返回（返回的是同一个 `bytes` 对象，连拷贝都没有）。所以照片类 meme 不会被 GIF 的 256 色调色板拉低画质。
- 覆盖范围：`/meme` 关键词触发、`/meme工坊随机`、`/meme工坊详情` 的预览图、`/sk`、`/sk随机`、`/sk 角色名` 的随机分支 —— 也就是内置库、`meme_emoji`、Gouqi 扩展和自制模板的全部出图路径。
- 有意不转换：`/meme工坊帮助`、`/meme工坊扩展列表`、`/sk表情`、`/sk角色` 这些列表长图本来就画在不透明白底上，没有 alpha 可言，白解码一遍纯属浪费；`/meme提取` 必须把用户的原始文件一个字节不改地送回去。
- 转换发生在「记住这张图」之前，所以最近记录和收藏指纹存的就是真正发出去的那张图，`引用 + meme收藏` 行为不变。
- 动图输出本来就是 GIF，本次顺带核对了它的透明通道编码，确认原本就正确，没有改动。
- 半透明像素按 alpha 128 取整成全透明或全不透明：GIF 每帧只能标记一个透明色，这是格式限制而非取舍。

**其他**

- `core/imaging.py` 抽出 `_frame_durations()` 与 `_encode_within_limit()`，透明 GIF 和普通 GIF 共用同一套「超过 20 MB 就缩帧重编」的收敛逻辑，`save_gif()` 行为不变。
- 新增 10 个单元测试，总数 183：透明 PNG 转 GIF 后逐像素核对镂空与主体色、不透明图/JPEG/GIF/坏字节原样返回、重复调用幂等、双帧 APNG 转 GIF 后保留 120/200 毫秒时长、配置关闭时不转换，以及 PJSK 出图「记住的字节 == 发出去的字节」。

## v1.10.3 - 2026-09-02

**修复：带参数的指令直接抛异常 —— `/sk 122 哟` 在聊天里没有任何回复**

- 现象：`/sk 122 哟` 发出去之后 Bot 一声不响，AstrBot 日志里是 `Plugin Handler arguments do not match its definition`，紧跟着 `pjsk_sticker_command() got an unexpected keyword argument 'args'`。
- 原因在插件这边，不是运行环境的问题：AstrBot 调用指令处理器时是**按关键字**传参的（`handler(event, **params)`），而它登记参数时会把 `self`、`event` 之后的所有形参都当成普通具名参数 —— 连 `*args` 这种可变参数也算在内，名字就叫 `args`。于是 `/sk 122 哟` 实际被调用成 `handler(event, selector=122, args='哟')`，而 Python 的 `*args` 接不了 `args=` 这个关键字，当场 TypeError。
- 影响范围：`/sk`、`/sk随机`、`/meme工坊自制新建` 三条指令只要带参数就必挂。不带参数也不正常 —— 4.27.x 会回一句「必要参数缺失」，4.26.x 更隐蔽，核心会把参数的类型名（字符串 `'str'`）当成值塞进来。这不是 4.27.5 的回归，4.26.x 上同样是坏的，只是这次终于报到了日志里。
- 修复：全部 31 条指令的处理器统一改成只声明 `(self, event)`，参数由插件自己从原始消息里切分（新增 `command_tail()`、`command_tokens()` 两个小工具：先把连续空白归一，再丢掉第一个词，也就是指令本身）。这样参数解析完全握在插件手里，AstrBot 以后再调整绑定规则也波及不到。
- 顺带修掉一个静默的类型 bug：核心会把「默认值为 None」的参数在取到纯数字时悄悄转成 `int`，`/meme工坊详情 666` 于是会把一个 `int` 交给按 `str | None` 写的代码。现在不经过核心解析，拿到的永远是字符串。
- 顺带一处体验修正：不带参数的 `/sk` 现在稳定给出用法说明，而不是抛「必要参数缺失」。

**修复：`/meme工坊取消收藏 sk 206` 一直没生效**

- README 里写了 PJSK 收藏要用「前缀 + 序号」两段来取消，但旧签名只拿得到第一个词 `sk`，`206` 被直接丢掉，于是永远匹配不到收藏项。现在整段参数都会交给解析函数，`/meme工坊取消收藏 sk 206` 与 `/meme工坊取消收藏 pjsk 206` 都能正常取消。普通 meme 的 `/meme工坊取消收藏 摸` 行为不变。

**其他**

- 新增 15 个单元测试，总数 173。其中一条是结构不变量测试：它直接读 `main.py` 的语法树，只要有人给 `@filter.command` 处理器加回额外形参就会失败，这个坑不会再踩第二次。另有测试把 AstrBot 两种签名读取模式（4.26.x 与 4.27.x）各跑一遍，用 18 条真实消息逐条核对参数切分结果。
- README 常见问题补充了这个报错的对应条目。

## v1.10.2 - 2026-09-02

**新增：`/sk角色` —— 用角色号翻角色，和表情序号彻底分开**

- 起因：`/sk角色` 之前只是 `/sk表情` 的一个别名，`/sk角色 16` 会被当成「表情序号 16 属于谁」来处理，想按「第 16 位角色」翻人做不到。
- 现在是三步：`/sk角色` 出 26 人总览图（每人头上的大号数字就是角色号）→ `/sk角色 16` 翻开 16 号角色、看清每张底图的表情序号 → `/sk 206 Wonderhoy` 出图。
- `/sk角色` 也认名字：`/sk角色 未来`、`/sk角色 miku` 与 `/sk角色 16` 完全等价。`/sk 角色 16` 这种把子命令写在 `/sk` 后面的形式同样可用，别名有 `/sk角色表`、`/SK角色`、`/pjsk角色`、`/pjsk角色表`、`/PJSK角色`。
- 出图部分一个字没改：`/sk`、`/sk表情`、`/sk随机` 后面的数字仍然是表情序号（1~359），群里的老习惯、收藏夹里存下的命令、WebUI 复制出来的指令都不受影响。
- 两套编号只在各自的指令后生效，写串了会明确提示而不是静默出错：`/sk角色 206` 会说明角色号只到 26，并告诉你 206 应该写成 `/sk 206 你的文字`（出图）或 `/sk表情 206`（看它属于哪位角色）。
- 总览图和角色页跟着重排：总览图每格是「大号角色号 + 角色名 + 表情序号区间」，角色页标题带上 `16. 初音未来`、副标题写明 `角色 16/26`，页脚提醒图上的数字是表情序号而不是角色号。

**修复：加载插件时的「重复触发词」警告**

- `/sk表情` 的别名里原本占着 `sk角色` 和 `pjsk角色`，现在这两个词归 `/sk角色` 自己所有，AstrBot 启动日志不再报 `重复触发词 sk角色`。

**其他**

- WebUI 的 PJSK 页左栏每位角色多了「角色 N」，选中卡片的说明也区分角色号和表情序号；搜索框里输入数字仍按表情序号理解，和聊天里的 `/sk` 保持一致。
- `/sk帮助`（以及不带参数的 `/sk`）换成三步式说明，并单独写明「角色号 1~26」和「表情序号 1~359」的区别。
- 新增 9 个单元测试：角色号命名空间、`/sk角色` 的数字与名称解析、两条子命令不再互相抢词，以及角色页指令的 6 种回复分支。测试总数 158。

## v1.10.1 - 2026-09-02

**修复：`/sk素材安装 确认` 只回一句「开始下载」就没了下文**

- 现象：管理员执行素材安装指令，Bot 回一条「开始下载」的提示，然后再没有任何消息，数据目录里一张底图都没有，日志里也不报错。
- 原因：这条指令在发出提示之前先调用了 `event.stop_event()`。AstrBot 的调度器处理异步生成器时，每 yield 一次就检查一遍事件是否已停止，一旦已停止就直接关闭生成器 —— 于是 `yield` 之后的下载代码根本没有机会执行。`/meme工坊资源检查`、`/meme工坊扩展更新`、`/meme工坊Gouqi安装` 三条指令是同样的写法，一并修好。
- 修复：中途的进度提示改用 `event.send()` 直接发送，不再占用 `yield`；`stop_event()` 挪到整个流程跑完之后。四条指令都补上了兜底的异常捕获，出错时把原因回给管理员，同时在日志里留完整堆栈，不再静默失败。
- 顺带改进：安装过程现在有三段反馈 —— 开始下载、下载完成（约 30 MB，开始逐个校验并解压）、最终结果。30 MB 在慢网络下要跑几分钟，中途有提示才知道它还在干活。

**改进：PJSK 指令前缀 `/pjsk` 简化为 `/sk`**

- 六条指令改名：`/sk表情`、`/sk`、`/sk随机`、`/sk帮助`、`/sk素材安装`、`/sk素材状态`，大写 `/SK…` 同样可用。
- 旧写法 `/pjsk…` 与 `/PJSK…` 全部保留为别名，群里的老习惯、收藏夹里已经存下的命令都不用改。
- 取消收藏同时认两种前缀：`/meme工坊取消收藏 sk 206` 与 `/meme工坊取消收藏 pjsk 206` 等价。
- README、WebUI 文案、`/sk帮助` 与故障排查段落同步更新。配置项名（`pjsk_enabled` 等）保持不变，升级不会丢配置。

**其他**

- 新增 6 个单元测试：安装进度回调的调用顺序、回调抛异常时安装仍能完成、无回调时不报错，以及 `sk` / `pjsk` 两种前缀的取消收藏解析。测试总数 149。

## v1.10.0 - 2026-09-02

**PJSK 表情工坊（可选）**

- 新增一套 Project Sekai 手写体表情包功能，交互按「看图选号」两步走：`/pjsk表情` 出 26 位角色的总览图（每人一张缩略图 + 序号区间），`/pjsk表情 未来` 出该角色全部姿势的缩略图（每张标着全局序号），然后 `/pjsk 206 Wonderhoy` 出图。参考的上游插件要先去列表里查 ID 再用 `-i 206` 传参，这里照着图报数字就行。
- 四种选择方式：全局序号 `206`、角色加编号 `未来3` / `miku3` / `MIKU 3`、分开写 `未来 3`、只写角色名 `未来`（从该角色里随机一张）。角色别名覆盖中文名、日文名、罗马字和常用缩写；认不出的写法会提示去看总览图，不会默默换一张图出。
- 排版参数 `-x` / `-y` 位置、`-r` 旋转（-10~10）、`-s` 字号（10~100）、`-l` 行距（0.8~3）、`-c` 弧形排版、`--scale` 输出倍数（1~4），全部带中文别名（`-横` `-纵` `-角度` `-字号` `-行距` `-弧形` `-倍数`），也认 `-s=34` 和全角数字。不写参数时用每张图的官方默认排版。
- 渲染全部在本机用 Pillow 完成，不装浏览器、不调外部接口：自动断行、超框自动缩字号、旋转、以及沿弧线逐字摆放的弧形排版都在插件内实现。文字里的 `\n` 换行，单次上限 6 行 / 160 字。
- 新指令 `/pjsk表情`、`/pjsk`、`/pjsk随机`、`/pjsk帮助`、`/pjsk素材状态` 和 `/pjsk素材安装 确认`（管理员），各自带常用别名；不带参数的 `/pjsk` 直接给用法速查。
- 与现有功能打通：PJSK 表情可以引用后 `/meme收藏`，收藏夹里给出 `/pjsk 206 你的文字` 形式的可复用命令，`/meme工坊取消收藏 pjsk 206` 移除；每次成功出图都会写入个人 / 本群 / 全局最近记录。
- 素材不随插件分发。管理员执行 `/pjsk素材安装 确认` 后才下载约 30 MB：359 张底图来自 MIT 许可的 `TheOriginalAyaka/sekai-stickers`，手写字体 `YurukaFangTang.ttf` 来自 MIT 许可的 `Agnes4m/nonebot_plugin_pjsk`。下载锁定在已审阅提交上，逐个文件校验相对路径、字节数和 Git blob 哈希，任何一项不符就整体失败，不会留半套素材。角色形象版权仍归 SEGA / Colorful Palette / Crypton Future Media。
- 新增配置项 `pjsk_enabled`（默认开）、`pjsk_download_timeout`（默认 900 秒）、`pjsk_output_scale`（默认 2 倍，底图 296×256）。关闭 `pjsk_enabled` 只是不注册指令，已下载的素材保留。

**WebUI 新增「PJSK」页**

- 三栏工作台：左栏是 26 位角色（主题色圆点 + 序号区间 + 按名称或序号搜索 + 「随便来一张」），中栏是实时预览、序号翻页和当前角色的缩略图网格（懒加载），右栏是文字与排版参数。
- 参数一改就自动重渲染（约半秒防抖）；`用这张的官方原句` 填回该图默认文案，`恢复默认` 回到原始排版。行数或字数超限会当场提示，不用等出图才发现被截断。
- 预览下方给出等价的聊天指令，一键复制回聊天里用；预览图可点开放大，也能直接下载 PNG。右栏底部列出素材来源、许可证、已审阅提交和安装时间，方便确认本机装的是哪一版。
- 总览页新增 PJSK 卡片，标签徽标显示贴纸数量。`pjsk_enabled` 关闭或素材未安装时，这一页显示明确的下一步操作而不是空白。
- 新增 4 个 Dashboard 接口：`dashboard/pjsk/status`、`characters`、`sticker`、`render`。

**其他**

- 新增 28 个单元测试（`tests/test_pjsk.py`），覆盖选择器解析、参数边界、排版计算、素材清单校验与 Dashboard 接口；测试总数 143。
- `/meme工坊更新检查` 的输出里增加一段 PJSK 素材状态（底图数量、已审阅提交、许可），缺素材时直接给出安装命令。

## v1.9.2 - 2026-08-29

**改进：主题选择器改为自绘菜单**

- 现象：点开顶部的主题选择器，弹出的列表比按钮还窄、没有内边距，当前项还被系统的浅蓝高亮盖住，几乎看不清字，和页面整体设计明显不搭。
- 原因：这里原来用的是原生 `<select>`。展开后的列表由浏览器/系统自己绘制，宽度、行高、内边距、选中项高亮色都不受页面 CSS 控制 —— v1.9.1 给 `option` 补的背景色只能改底色，改不了这些。
- 修复：换成插件自绘的 listbox。弹层与按钮右边缘对齐、固定 238px 宽，每套主题一枚渐变色点 + 主题名 + 明暗与色相说明（如「深色 · 青绿」），当前项用主题强调色和勾选标记标出。
- 弹层背景用各主题的 `--menu-bg`（唯一一个不透明的主题色），不会透出后面的卡片。
- 键盘可用：`↑`/`↓` 展开并在选项间循环移动，`Home`/`End` 跳到首尾，`Enter`/`Space` 选中，`Esc` 关闭并把焦点还给按钮，`Tab` 关闭并继续向后跳；点击页面别处只关菜单、不抢焦点。语义上使用 `role="listbox"` / `role="option"` 与 `aria-expanded` / `aria-activedescendant`。
- 六套主题都实测过对比度：主题名与弹层底色的对比度 5.4~10.4，说明文字 3.8~4.7。

**修复：窄屏下主题菜单会被裁掉**

- 主题选择器是顶栏三个按钮里最左边那个，右对齐的弹层在 380px 宽度下会超出屏幕左边缘约 8px。
- 现在 620px 以下改为向右展开（`left: 0`），并把弹层宽度限制在 `calc(100vw - 2 * 边距)` 以内。

**修复：窄屏顶栏文字折行**

- 380px 下「Meme 工坊」被折成三行，「宽松」「刷新」也被折成两行。
- 顶部按钮统一 `white-space: nowrap`，标题超长时改为省略号。

## v1.9.1 - 2026-08-29

**修复：WebUI 首屏没有主题（白板/黑板）**

- 现象：每次刚打开「Meme 工坊」页面都是无样式的黑底白字，手动切一次主题才恢复正常。
- 原因：AstrBot 会接管插件页的 `<html data-theme>`——`dashboard/services/plugin_page_service.py` 的 `apply_theme_to_html` 在下发 HTML 时删掉页面自带的 `data-theme` 并改写成宿主的 `dark`/`light`，`plugin_page_bridge.js` 的 `applyContext` 在初始上下文和每次宿主换主题时又会再写一遍。本页六套主题原本都定义在 `:root[data-theme="…"]` 上，被改写成 `dark` 后一条都匹配不上，所有主题变量变成未定义，`var()` 全部失效，于是卡片底色、边框、阴影统统回落到初始值。
- 修复：主题改挂在插件私有属性 `data-mf-theme` / `data-mf-density` 上，宿主再怎么写 `data-theme` 都不受影响；同时把「极光」主题同时定义在裸 `:root` 上，页面在任何情况下都不可能处于「无主题」状态。
- 顺带把首屏主题恢复提前到 `<head>` 内联脚本，在第一次绘制前就从本地记录还原主题与密度，避免闪一下默认配色。
- 没有手动选过主题时，页面会跟随 AstrBot 宿主的明暗模式（宿主暗色→极光，亮色→日光）；一旦自己在下拉框里选过，就固定用自己的选择，不再被宿主带着走。

**修复：主题下拉框白底白字**

- 深色主题下原生 `<select>` 的展开菜单是系统绘制的，页面只把控件本身设成透明背景 + 继承文字色，没有给 `option` 任何样式，展开后就成了白底白字。
- 现在六套主题各自提供 `--menu-bg`，并统一为 `option` / `optgroup` 指定背景与文字色。

**修复：插件列表不显示图标**

- AstrBot 只认插件根目录下的 `logo.png`（`astrbot/core/star/star_manager.py`：`logo_fname = "logo.png"`，拼在插件目录上，存在才写入 `metadata.logo_path`），没有任何回退路径。本插件的图标此前只放在 `assets/icon.png`，所以插件市场/已安装列表里一直是空白。
- 现在插件根目录提供 `logo.png`（512×512，与 `assets/icon.png` 同一枚徽章）。

**修复：版本号显示的是引擎版本**

- 首屏标题旁和底部状态栏的 `v0.2.3` 其实是 `meme_generator` 的引擎版本，容易被误认成插件版本。
- `dashboard/overview` 新增 `plugin_version` 字段（从 `metadata.yaml` 读一次并缓存，不引入 YAML 依赖），标题与状态栏改用插件版本，引擎版本仍在「引擎」卡片里单独一行显示。

## v1.9.0 - 2026-08-29

**自制表情包（Meme Maker）**

- 新增 Meme Maker：一个模板 = 一块画布 + 图层，图层分为运行时填图的「图片位」和运行时填字的「文字位」。模板保存后立即热加载成正常 meme，可被触发、随机、收藏、禁用，并出现在详情和最近记录里。
- 图片位支持填充方式（裁切填满/完整包含/拉伸）、圆角、圆形裁切、旋转、不透明度、水平翻转、灰度，以及画在底图之下（`behind_base`）；文字位支持默认文本、颜色、描边、字号与最小字号、粗体、水平/垂直对齐、旋转、行距、最大行数和强制大写。
- 输入含动图时输出 GIF，全静态输入输出 PNG；渲染受画布边长 64-2048、总像素 4,000,000、单素材 8 MB、模板 200 个、单模板 16 图层（图片位 4 / 文字位 8）等上限约束。
- 新增聊天命令 `/meme工坊自制列表`、`/meme工坊自制新建`、`/meme工坊自制删除`、`/meme工坊自制重载`（后三个为管理员），引用一张图片即可用 `自制新建` 生成底部字幕模板。
- 新增 6 个 Dashboard 接口（`dashboard/maker/templates|template|scaffold|preview|save|delete`），保存与删除后自动热加载，无需重启 AstrBot。
- 新增配置项 `maker_enabled`：关闭只是从运行时取消注册，模板文件保留。
- 抽出共享成像层 `core/imaging.py`，Gouqi 兼容渲染与 Meme Maker 复用同一套解码、动图合帧、字体加载、自适应文字和圆角遮罩实现，避免两处各写一遍。

**Dashboard WebUI 框架重置**

- 不再是换配色：去掉侧边栏，改成顶部品牌栏 + 横向标签导航 + 限宽内容区 + 底部状态栏，全屏和窄屏都能看到完整功能。
- 主题从 3 套扩展到 6 套（极光 / 午夜 / 碳墨 / 紫梅 / 日光 / 米纸，4 暗 2 亮），新增宽松/紧凑信息密度切换；主题与密度记在浏览器本地。
- 新增「工作台」标签：左侧模板列表、中间按真实比例缩放的画布、右侧属性面板；画布可拖动缩放图层、方向键微调、`Delete` 删除、开关对齐网格，并提供占位图真实渲染预览。
- 状态栏显示引擎版本与库存统计，并带滚动进度指示。
- 删除模板等破坏性操作改用页面内确认弹窗，不再使用浏览器原生 `confirm`。

**插件 Logo**

- 新增 `assets/icon.svg`、`assets/logo.svg`、`assets/icon.png`（512×512）：深色圆角徽章 + 开口薄荷色环 + 缺口处的琥珀色四角星，在浅色和深色背景上都能辨识。
- WebUI 顶栏图标同步换成同一枚标记，README 顶部展示文字版 logo。

**修复**

- 修复表情详情抽屉只显示各段标题、正文被裁切的问题：抽屉滚动区是定高网格，而卡片是 `overflow: hidden`，其自动最小尺寸为 0，导致 `auto` 行被均分压扁；现在显式使用 `grid-auto-rows: max-content`。
- 同样加固工作台的模板列表与属性面板，避免内容较多时被同一机制压扁。
- 修复关闭 `maker_enabled` 时「工作台」标签仍显示模板数量的问题，现在显示「关」并使用弱化样式。

**文档**

- README 重写 Dashboard 章节、新增「自制表情包（Meme Maker）」章节、新增自制模板的常见问题，并补齐命令表与配置表。
- `THIRD_PARTY_NOTICES.md` 新增「Evaluated but not used」：记录 `dionaka/meme_maker` 与 `kamicry/astrbot_plugin_apiver_meme_drawer_arcaea_pjsk` 的评估结论和不采用原因（许可不明或声明冲突、依赖用户自建外部接口、随包分发第三方版权素材）。

## v1.8.0 - 2026-08-27

- Dashboard WebUI 完全重做：不再只是换配色，而是重建了一套基于设计令牌的框架，包含 **Nocturne（夜间）/ Daylight（浅色）** 两套完整主题与「跟随系统」选项，主题选择保存在浏览器本地。
- 布局改为窄侧边栏 + 三视图工作区（概览 / 表情库 / 最近记录），支持地址栏 `#overview`、`#library`、`#records` 直接进入；中宽屏侧边栏收成图标栏，手机宽度改为底部标签栏。
- 表情详情从内嵌区块改为右侧抽屉（窄屏为底部抽屉），提供可复制指令示例、按需预览、参数说明、素材缩略图与素材灯箱；素材缩略图懒加载并限制并发。
- 概览新增来源分布、启用比例条、扩展状态卡、常用 meme 排行与最近活动时间线，卡片快捷入口可带筛选条件跳转到对应视图。
- 表情库新增来源筛选与排序（key、图片数、文字数、来源）、网格/列表两种密度、多选批量启停与「本页全选」，单次批量上限 200 个 meme。
- 新增批量启停接口 `dashboard/memes-enabled`，并为配置写入加锁，避免并发启停互相覆盖。
- 修复扩展未安装时状态卡因空 class 名抛错、导致整个扩展面板空白的问题。
- 修复主题切换中一处把视图名误判为主题名的赋值、批量按钮在请求期间可重复点击的问题。
- 修复 `/` 快捷键在抽屉或灯箱打开时抢走输入焦点的问题。
- 修复筛选后停留在越界分页时列表显示为空的问题，现在会自动回到第一页。
- 概览与列表的标签、来源统计改为基于全部 meme 聚合，筛选后筛选器选项不再自行消失。
- 修复窗口高度不足时整页被内容顶高、侧边栏底部的主题切换器滚不到、点不到的问题：外壳与工作区改为固定高度网格，侧边栏自身可滚动。
- 修复表情详情抽屉内容高于视口时被裁切且无法滚动的问题（抽屉网格缺少显式行高，隐式行会被内容撑开）；现在抽屉固定为视口高度，内部区域独立滚动。
- 重做素材灯箱：图片与说明文字合并进同一 `figure` 容器居中显示，说明文字改为紧贴图片下方的等宽标签，不再被钉在屏幕最底部；透明素材使用棋盘格底衬。
- 修复侧边栏导航项在计数徽标较大时文字被截断的问题，徽标数字改为紧凑写法（如 `1.3k`）。
- 修复表情卡片底部来源徽标与图文需求信息在窄卡片内换行错位的问题，需求信息合并为一行紧凑说明并带完整悬浮提示。
- 修复搜索框内 `/` 快捷键提示溢出输入框、以及 1240px 以下占位文字被裁切的问题，搜索框宽度固定为 240px 且不再把工具栏挤成两行。
- 修复扩展状态卡说明文字过长时被截断的问题。
- 统一四个指标卡的进度条样式与含义，去掉概览头部重复出现的状态标签。
- 抽屉的四个指标块与窄屏下的概览指标卡改为稳定 2×2 排列，不再出现 3+1 的错位布局。
- 新增 `--rail-bg`、`--topbar-bg` 等表面令牌，浅色主题下侧边栏与主区形成明确层级。

## v1.7.1 - 2026-08-17

- 新增 `/meme工坊扩展列表`（短别名 `/meme扩展列表`），只生成当前已加载扩展 meme 的列表图，同时包含 `meme_emoji`/其他原生外部包与 Gouqi 兼容模板。
- 使用隔离的临时 `MEME_HOME` 读取当前 `meme_generator` 内置 key，再与实际运行时注册表比较；无需硬编码 `meme_emoji` 的数百个 key，扩展更新新增模板后也能自动进入列表。
- Dashboard 现在会把原生外部扩展标记为“原生外部扩展”，不再统一显示成 `meme_generator`。

## v1.7.0 - 2026-08-17

- 新增可选 Gouqi 扩展，手动兼容 `amalopyy123/meme-generator-gouqi` 当前 10 个模板；安装后会接入现有参数收集、随机、收藏、最近记录、禁用和 Dashboard 管理。
- 上游使用已淘汰的 Python `add_meme` API，因此没有直接执行其源码；改为本插件内受输入像素、动画帧数和输出大小限制的 Pillow 渲染适配层。
- 新增 `/meme工坊Gouqi扩展状态` 与管理员 `/meme工坊Gouqi扩展安装 确认`，安装成功后热加载，无需重启 AstrBot；同时提供“枸杞”中文别名和安装/更新兼容别名。
- 安装器固定到人工审阅提交 `40eb41cf7c30`，只解压 31 个明确列出的图片/GIF，逐项校验路径、大小和 Git blob 哈希；忽略上游 `.py`、`.pyc` 和未使用的重复素材。
- `/meme工坊更新检查` 增加 Gouqi 本地完整性、插件审阅提交和上游最新提交检查；发现未审阅的新提交时只报告，不自动下载或执行。
- Dashboard 概览同时显示两个扩展状态，表情详情标注来源，并支持查看 Gouqi 素材；帮助列表图也会附加 Gouqi 模板清单。
- 修复 Gouqi 上游 `ceshi` 模板把必填文字绘制到当前背景图片范围之外、实际不可见的问题。
- 上游当前未声明开源许可证，因此本插件不打包其源码和素材；安装前会明确要求部署者确认已取得作者和素材使用授权。
- 修复扩展注册遇到重复 meme key 时键索引与触发词可能指向不同实现的问题；现在保留先加载实现并跳过冲突扩展项。

## v1.6.0 - 2026-08-16

- 新增管理员命令 `/meme工坊更新检查`（别名 `/meme更新检查`），只读检查内置 `meme_generator` 与 `meme_emoji` 扩展版本，不会下载、安装或修改配置。
- 内置引擎检查从 PyPI 版本列表中选择插件兼容范围 `>=0.2.3,<0.3` 内的最新稳定版本，避免把未来不兼容的 `0.3.x` 误报为可直接更新。
- 扩展检查同时显示本地安装版本、当前平台的最新 Release 和本地动态库/许可证/资源校验状态；存在更新或安装不完整时给出明确更新命令与重启提示。
- PyPI、GitHub Release 和本地扩展状态并行检查，单项最长等待 20 秒；任一来源失败时仍返回其他可用结果。
- 明确区分版本检查与素材检查：只读更新检查不会遍历或下载内置素材，需要补齐素材时仍使用 `/meme工坊资源检查`。

## v1.5.0 - 2026-08-16

- 手动融合上游 memelite v3.0.5 的 QQ 官方机器人适配思路，支持 `qq_official` 与 `qq_official_webhook` 使用 `appid + openid` 获取发送者和被 At 用户头像。
- 支持参数中的 `<@openid>`、`<@!openid>`，以及 QQ 官方平台消息中的 `@openid`；消息链 At 与文字 At 会去重，并优先保留适配器提供的用户昵称。
- 新增有上限的头像 LRU 内存缓存，默认保留最近 20 个 QQ/QQ 官方头像；可通过 `avatar_cache_size` 调整或设为 `0` 关闭，插件终止时会清空。
- 头像下载继续强制使用 HTTPS，并复用原有网络超时、响应状态检查和大小限制；单个头像另设 2 MiB 上限。未采用上游的 HTTPS 降级和非数字 ID 随机 QQ 号回退。
- 核对上游近期 Issues：方向参数、`meme_generator` 导入恢复和资源检查卡住等问题已由 Meme 工坊现有参数层、0.2.x 兼容层及可终止子进程覆盖，无需引入旧版实现。

## v1.4.1 - 2026-08-10

- 表情提取支持单次发送模式覆盖：`/meme提取 图片` 强制静态图以图片消息发送，`/meme提取 文件` 强制以文件发送；主指令与兼容别名均可使用。
- 非法模式会返回明确用法；GIF、APNG、WebP 等动画表情即使指定“图片”仍以文件发送，避免平台丢失动画帧。

## v1.4.0 - 2026-08-08

- Dashboard 改为紧凑侧栏导航，拆分为“概览 / 表情库 / 最近记录”三个独立工作视图，不再需要在一条长页面中来回滚动。
- 新增概览洞察：展示常用 meme、活跃会话和最近活动；可从概览直接跳转到相应表情的参数页或会话记录。
- 概览汇总复用已有的持久化使用记录，只返回 meme 标识、触发词、会话、用户和时间，不保存或传输图片内容。
- 侧栏宽度控制为 208px；窄屏自动切换为顶部紧凑导航，并保持浅色、夜间主题和现有管理能力。

## v1.3.0 - 2026-08-08

- 新增 AstrBot Dashboard Plugin Page：可搜索和筛选运行时表情库、单独启用/禁用 meme、查看运行时参数、按需生成预览，以及查看本地素材图片。
- 新增 Dashboard 最近生成记录，可查看全局记录或按会话筛选群友/私聊参与者的触发记录；不返回或保存生成图片。
- 新增 `/meme工坊本群最近`、管理员 `/meme工坊全局最近`，并持久化有上限的成功生成记录。
- 合入并重构 `XTsat/astrbot_plugin_meme_grabber` 的表情提取能力：支持当前消息、引用图片、QQ OneBot `mface`/官方表情回退、动画文件发送和延迟清理。
- 新增提取功能的开关、群白/黑名单、发送方式、文件数量和保留时间配置。
- 修正素材列表被截断时错误显示总数的问题；WebUI 现在报告真实素材总数和“仅显示前 60 个”状态。
- 补充 `XTsat/astrbot_plugin_meme_grabber` 的 AGPL-3.0 来源声明、公开 README、Dashboard 隐私边界与收藏持久化说明。

## v1.2.0 - 2026-08-06

- 增加用户独立、跨重启持久化的 meme 收藏夹；引用 Bot 生成的图片并回复 `/meme收藏` 即可添加。
- 增加 `/meme收藏夹` 和 `/meme取消收藏 <触发词>`，收藏列表直接给出当前配置下可复用的生成命令。
- 普通生成、随机生成和 `meme_emoji` 扩展 meme 都会写入可收藏索引。
- 使用 SHA-256 与 256 位感知指纹识别被引用图片，可兼容常见平台的缩放和重新编码；只保存指纹与 meme 标识，不保存图片内容。
- 增加 `max_favorites` 配置，默认每个用户最多保存 50 项收藏。

## v1.1.0 - 2026-07-31

- 增加 `/meme工坊随机`（别名 `/随机meme`），从当前已加载且未禁用的完整 meme 列表中随机生成表情包。
- 随机命令复用统一的输入收集、并发限制、超时处理和输出压缩流程；安装 `meme_emoji` 扩展后无需额外配置即可参与随机选择。
- 增加 `/meme工坊最近`（别名 `/最近meme`），按发送者和平台显示当前运行期间最近触发的 3 个不同 meme。

## v1.0.0 - 2026-07-30

- 使用独立的插件、类、命令、日志和数据目录标识，默认以 `meme` 作为隔离触发前缀。
- 根据每个 meme 的运行时参数定义解析自然别名、`key=value` 和命令行风格选项。
- 修复 `奶茶 左手`、`对称 上` 等选项被当作普通文本或直接忽略的问题。
- 修复回复图片优先级、引用文本、显式 `name`/`gender` 被覆盖和额外前缀混入正文的问题。
- 兼容 `meme_generator` 0.2.3 的导入与对象 API，规避临时 namespace module 导致的导入失败。
- 将资源检查放入可终止的子进程，避免启动检查无限挂起。
- 增加 anyliew/meme_emoji 的当前 Rust 兼容扩展安装、校验、资源同步和状态命令。
- 增加 HTTPS 下载、输入大小限制、网络超时、并发限制和静态输出图压缩。
- 修正 AstrBot WebUI 的别名模板结构，并兼容 `file:///` 图片与仅含纯文本字段的引用消息。
- 合并消息链和正文中的重复 @目标，避免头像重复，并保持每张头像对应的昵称。
- 扩展资源先完整暂存再合并；写入失败时回滚已有文件，同时核对并保存上游许可证。
