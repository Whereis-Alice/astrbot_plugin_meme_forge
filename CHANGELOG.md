# Changelog

## v1.9.0 - 2026-08-29

**自制表情包（Meme Maker）**

- 新增 Meme Maker：一个模板 = 一块画布 + 图层，图层分为运行时填图的「图片位」和运行时填字的「文字位」。模板保存后立即热加载成正常 meme，可被触发、随机、收藏、禁用，并出现在详情和最近记录里。
- 图片位支持填充方式（裁切填满/完整包含/拉伸）、圆角、圆形裁切、旋转、不透明度、水平翻转、灰度，以及画在底图之下（`behind_base`）；文字位支持默认文本、颜色、描边、字号与最小字号、粗体、水平/垂直对齐、旋转、行距、最大行数和强制大写。
- 输入含动图时输出 GIF，全静态输入输出 PNG；渲染受画布边长 64-2048、总像素 4,000,000、单素材 8 MB、模板 200 个、单模板 16 图层（图片位 4 / 文字位 8）等上限约束。
- 新增聊天命令 `/meme工坊自制列表`、`/meme工坊自制新建`、`/meme工坊自制删除`、`/meme工坊自制重载`（后三个为管理员），引用一张图片即可用 `自制新建` 生成底部字幕模板。
- 新增 6 个 Dashboard 接口（`dashboard/maker/templates|template|scaffold|preview|save|delete`），保存与删除后自动热加载，无需重启 AstrBot。
- 新增配置项 `maker_enabled`：关闭只是从运行时取消注册，模板文件保留。
- 抽出共享成像层 `core/imaging.py`，Gouqi 兼容渲染与 Meme Maker 复用同一套解码、动图合帧、字体加载、自适应文字和圆角遮罩实现，避免两处各写一遍。

**Dashboard WebUI 框架重置**

- 不再是换配色：去掉侧边栏，改成顶部品牌栏 + 横向标签导航 + 限宽内容区 + 底部状态栏，全屏和窄屏都能看到完整功能。
- 主题从 3 套扩展到 6 套（极光 / 午夜 / 碳墨 / 紫梅 / 日光 / 米纸，4 暗 2 亮），新增宽松/紧凑信息密度切换；主题与密度记在浏览器本地。
- 新增「工作台」标签：左侧模板列表、中间按真实比例缩放的画布、右侧属性面板；画布可拖动缩放图层、方向键微调、`Delete` 删除、开关对齐网格，并提供占位图真实渲染预览。
- 状态栏显示引擎版本与库存统计，并带滚动进度指示。
- 删除模板等破坏性操作改用页面内确认弹窗，不再使用浏览器原生 `confirm`。

**插件 Logo**

- 新增 `assets/icon.svg`、`assets/logo.svg`、`assets/icon.png`（512×512）：深色圆角徽章 + 开口薄荷色环 + 缺口处的琥珀色四角星，在浅色和深色背景上都能辨识。
- WebUI 顶栏图标同步换成同一枚标记，README 顶部展示文字版 logo。

**修复**

- 修复表情详情抽屉只显示各段标题、正文被裁切的问题：抽屉滚动区是定高网格，而卡片是 `overflow: hidden`，其自动最小尺寸为 0，导致 `auto` 行被均分压扁；现在显式使用 `grid-auto-rows: max-content`。
- 同样加固工作台的模板列表与属性面板，避免内容较多时被同一机制压扁。
- 修复关闭 `maker_enabled` 时「工作台」标签仍显示模板数量的问题，现在显示「关」并使用弱化样式。

**文档**

- README 重写 Dashboard 章节、新增「自制表情包（Meme Maker）」章节、新增自制模板的常见问题，并补齐命令表与配置表。
- `THIRD_PARTY_NOTICES.md` 新增「Evaluated but not used」：记录 `dionaka/meme_maker` 与 `kamicry/astrbot_plugin_apiver_meme_drawer_arcaea_pjsk` 的评估结论和不采用原因（许可不明或声明冲突、依赖用户自建外部接口、随包分发第三方版权素材）。

## v1.8.0 - 2026-08-27

- Dashboard WebUI 完全重做：不再只是换配色，而是重建了一套基于设计令牌的框架，包含 **Nocturne（夜间）/ Daylight（浅色）** 两套完整主题与「跟随系统」选项，主题选择保存在浏览器本地。
- 布局改为窄侧边栏 + 三视图工作区（概览 / 表情库 / 最近记录），支持地址栏 `#overview`、`#library`、`#records` 直接进入；中宽屏侧边栏收成图标栏，手机宽度改为底部标签栏。
- 表情详情从内嵌区块改为右侧抽屉（窄屏为底部抽屉），提供可复制指令示例、按需预览、参数说明、素材缩略图与素材灯箱；素材缩略图懒加载并限制并发。
- 概览新增来源分布、启用比例条、扩展状态卡、常用 meme 排行与最近活动时间线，卡片快捷入口可带筛选条件跳转到对应视图。
- 表情库新增来源筛选与排序（key、图片数、文字数、来源）、网格/列表两种密度、多选批量启停与「本页全选」，单次批量上限 200 个 meme。
- 新增批量启停接口 `dashboard/memes-enabled`，并为配置写入加锁，避免并发启停互相覆盖。
- 修复扩展未安装时状态卡因空 class 名抛错、导致整个扩展面板空白的问题。
- 修复主题切换中一处把视图名误判为主题名的赋值、批量按钮在请求期间可重复点击的问题。
- 修复 `/` 快捷键在抽屉或灯箱打开时抢走输入焦点的问题。
- 修复筛选后停留在越界分页时列表显示为空的问题，现在会自动回到第一页。
- 概览与列表的标签、来源统计改为基于全部 meme 聚合，筛选后筛选器选项不再自行消失。
- 修复窗口高度不足时整页被内容顶高、侧边栏底部的主题切换器滚不到、点不到的问题：外壳与工作区改为固定高度网格，侧边栏自身可滚动。
- 修复表情详情抽屉内容高于视口时被裁切且无法滚动的问题（抽屉网格缺少显式行高，隐式行会被内容撑开）；现在抽屉固定为视口高度，内部区域独立滚动。
- 重做素材灯箱：图片与说明文字合并进同一 `figure` 容器居中显示，说明文字改为紧贴图片下方的等宽标签，不再被钉在屏幕最底部；透明素材使用棋盘格底衬。
- 修复侧边栏导航项在计数徽标较大时文字被截断的问题，徽标数字改为紧凑写法（如 `1.3k`）。
- 修复表情卡片底部来源徽标与图文需求信息在窄卡片内换行错位的问题，需求信息合并为一行紧凑说明并带完整悬浮提示。
- 修复搜索框内 `/` 快捷键提示溢出输入框、以及 1240px 以下占位文字被裁切的问题，搜索框宽度固定为 240px 且不再把工具栏挤成两行。
- 修复扩展状态卡说明文字过长时被截断的问题。
- 统一四个指标卡的进度条样式与含义，去掉概览头部重复出现的状态标签。
- 抽屉的四个指标块与窄屏下的概览指标卡改为稳定 2×2 排列，不再出现 3+1 的错位布局。
- 新增 `--rail-bg`、`--topbar-bg` 等表面令牌，浅色主题下侧边栏与主区形成明确层级。

## v1.7.1 - 2026-08-17

- 新增 `/meme工坊扩展列表`（短别名 `/meme扩展列表`），只生成当前已加载扩展 meme 的列表图，同时包含 `meme_emoji`/其他原生外部包与 Gouqi 兼容模板。
- 使用隔离的临时 `MEME_HOME` 读取当前 `meme_generator` 内置 key，再与实际运行时注册表比较；无需硬编码 `meme_emoji` 的数百个 key，扩展更新新增模板后也能自动进入列表。
- Dashboard 现在会把原生外部扩展标记为“原生外部扩展”，不再统一显示成 `meme_generator`。

## v1.7.0 - 2026-08-17

- 新增可选 Gouqi 扩展，手动兼容 `amalopyy123/meme-generator-gouqi` 当前 10 个模板；安装后会接入现有参数收集、随机、收藏、最近记录、禁用和 Dashboard 管理。
- 上游使用已淘汰的 Python `add_meme` API，因此没有直接执行其源码；改为本插件内受输入像素、动画帧数和输出大小限制的 Pillow 渲染适配层。
- 新增 `/meme工坊Gouqi扩展状态` 与管理员 `/meme工坊Gouqi扩展安装 确认`，安装成功后热加载，无需重启 AstrBot；同时提供“枸杞”中文别名和安装/更新兼容别名。
- 安装器固定到人工审阅提交 `40eb41cf7c30`，只解压 31 个明确列出的图片/GIF，逐项校验路径、大小和 Git blob 哈希；忽略上游 `.py`、`.pyc` 和未使用的重复素材。
- `/meme工坊更新检查` 增加 Gouqi 本地完整性、插件审阅提交和上游最新提交检查；发现未审阅的新提交时只报告，不自动下载或执行。
- Dashboard 概览同时显示两个扩展状态，表情详情标注来源，并支持查看 Gouqi 素材；帮助列表图也会附加 Gouqi 模板清单。
- 修复 Gouqi 上游 `ceshi` 模板把必填文字绘制到当前背景图片范围之外、实际不可见的问题。
- 上游当前未声明开源许可证，因此本插件不打包其源码和素材；安装前会明确要求部署者确认已取得作者和素材使用授权。
- 修复扩展注册遇到重复 meme key 时键索引与触发词可能指向不同实现的问题；现在保留先加载实现并跳过冲突扩展项。

## v1.6.0 - 2026-08-16

- 新增管理员命令 `/meme工坊更新检查`（别名 `/meme更新检查`），只读检查内置 `meme_generator` 与 `meme_emoji` 扩展版本，不会下载、安装或修改配置。
- 内置引擎检查从 PyPI 版本列表中选择插件兼容范围 `>=0.2.3,<0.3` 内的最新稳定版本，避免把未来不兼容的 `0.3.x` 误报为可直接更新。
- 扩展检查同时显示本地安装版本、当前平台的最新 Release 和本地动态库/许可证/资源校验状态；存在更新或安装不完整时给出明确更新命令与重启提示。
- PyPI、GitHub Release 和本地扩展状态并行检查，单项最长等待 20 秒；任一来源失败时仍返回其他可用结果。
- 明确区分版本检查与素材检查：只读更新检查不会遍历或下载内置素材，需要补齐素材时仍使用 `/meme工坊资源检查`。

## v1.5.0 - 2026-08-16

- 手动融合上游 memelite v3.0.5 的 QQ 官方机器人适配思路，支持 `qq_official` 与 `qq_official_webhook` 使用 `appid + openid` 获取发送者和被 At 用户头像。
- 支持参数中的 `<@openid>`、`<@!openid>`，以及 QQ 官方平台消息中的 `@openid`；消息链 At 与文字 At 会去重，并优先保留适配器提供的用户昵称。
- 新增有上限的头像 LRU 内存缓存，默认保留最近 20 个 QQ/QQ 官方头像；可通过 `avatar_cache_size` 调整或设为 `0` 关闭，插件终止时会清空。
- 头像下载继续强制使用 HTTPS，并复用原有网络超时、响应状态检查和大小限制；单个头像另设 2 MiB 上限。未采用上游的 HTTPS 降级和非数字 ID 随机 QQ 号回退。
- 核对上游近期 Issues：方向参数、`meme_generator` 导入恢复和资源检查卡住等问题已由 Meme 工坊现有参数层、0.2.x 兼容层及可终止子进程覆盖，无需引入旧版实现。

## v1.4.1 - 2026-08-10

- 表情提取支持单次发送模式覆盖：`/meme提取 图片` 强制静态图以图片消息发送，`/meme提取 文件` 强制以文件发送；主指令与兼容别名均可使用。
- 非法模式会返回明确用法；GIF、APNG、WebP 等动画表情即使指定“图片”仍以文件发送，避免平台丢失动画帧。

## v1.4.0 - 2026-08-08

- Dashboard 改为紧凑侧栏导航，拆分为“概览 / 表情库 / 最近记录”三个独立工作视图，不再需要在一条长页面中来回滚动。
- 新增概览洞察：展示常用 meme、活跃会话和最近活动；可从概览直接跳转到相应表情的参数页或会话记录。
- 概览汇总复用已有的持久化使用记录，只返回 meme 标识、触发词、会话、用户和时间，不保存或传输图片内容。
- 侧栏宽度控制为 208px；窄屏自动切换为顶部紧凑导航，并保持浅色、夜间主题和现有管理能力。

## v1.3.0 - 2026-08-08

- 新增 AstrBot Dashboard Plugin Page：可搜索和筛选运行时表情库、单独启用/禁用 meme、查看运行时参数、按需生成预览，以及查看本地素材图片。
- 新增 Dashboard 最近生成记录，可查看全局记录或按会话筛选群友/私聊参与者的触发记录；不返回或保存生成图片。
- 新增 `/meme工坊本群最近`、管理员 `/meme工坊全局最近`，并持久化有上限的成功生成记录。
- 合入并重构 `XTsat/astrbot_plugin_meme_grabber` 的表情提取能力：支持当前消息、引用图片、QQ OneBot `mface`/官方表情回退、动画文件发送和延迟清理。
- 新增提取功能的开关、群白/黑名单、发送方式、文件数量和保留时间配置。
- 修正素材列表被截断时错误显示总数的问题；WebUI 现在报告真实素材总数和“仅显示前 60 个”状态。
- 补充 `XTsat/astrbot_plugin_meme_grabber` 的 AGPL-3.0 来源声明、公开 README、Dashboard 隐私边界与收藏持久化说明。

## v1.2.0 - 2026-08-06

- 增加用户独立、跨重启持久化的 meme 收藏夹；引用 Bot 生成的图片并回复 `/meme收藏` 即可添加。
- 增加 `/meme收藏夹` 和 `/meme取消收藏 <触发词>`，收藏列表直接给出当前配置下可复用的生成命令。
- 普通生成、随机生成和 `meme_emoji` 扩展 meme 都会写入可收藏索引。
- 使用 SHA-256 与 256 位感知指纹识别被引用图片，可兼容常见平台的缩放和重新编码；只保存指纹与 meme 标识，不保存图片内容。
- 增加 `max_favorites` 配置，默认每个用户最多保存 50 项收藏。

## v1.1.0 - 2026-07-31

- 增加 `/meme工坊随机`（别名 `/随机meme`），从当前已加载且未禁用的完整 meme 列表中随机生成表情包。
- 随机命令复用统一的输入收集、并发限制、超时处理和输出压缩流程；安装 `meme_emoji` 扩展后无需额外配置即可参与随机选择。
- 增加 `/meme工坊最近`（别名 `/最近meme`），按发送者和平台显示当前运行期间最近触发的 3 个不同 meme。

## v1.0.0 - 2026-07-30

- 使用独立的插件、类、命令、日志和数据目录标识，默认以 `meme` 作为隔离触发前缀。
- 根据每个 meme 的运行时参数定义解析自然别名、`key=value` 和命令行风格选项。
- 修复 `奶茶 左手`、`对称 上` 等选项被当作普通文本或直接忽略的问题。
- 修复回复图片优先级、引用文本、显式 `name`/`gender` 被覆盖和额外前缀混入正文的问题。
- 兼容 `meme_generator` 0.2.3 的导入与对象 API，规避临时 namespace module 导致的导入失败。
- 将资源检查放入可终止的子进程，避免启动检查无限挂起。
- 增加 anyliew/meme_emoji 的当前 Rust 兼容扩展安装、校验、资源同步和状态命令。
- 增加 HTTPS 下载、输入大小限制、网络超时、并发限制和静态输出图压缩。
- 修正 AstrBot WebUI 的别名模板结构，并兼容 `file:///` 图片与仅含纯文本字段的引用消息。
- 合并消息链和正文中的重复 @目标，避免头像重复，并保持每张头像对应的昵称。
- 扩展资源先完整暂存再合并；写入失败时回滚已有文件，同时核对并保存上游许可证。
